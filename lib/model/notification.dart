class Notifications {
  String title, subTitle, status;
  bool isUnread;

  Notifications({
    required this.title,
    required this.subTitle,
    required this.status,
    required this.isUnread,
  });
}
